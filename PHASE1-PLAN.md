# Glyphs — Phase 1 Plan (Electron shell & containment)

This is the approved implementation plan for Phase 1. ROADMAP.md says *what*, DESIGN.md says *why*, this file says *how*. The Phase 1 audit checks the code on disk against this document.

## Scope

Exactly the Phase 1 "Build" list in ROADMAP.md: fullscreen kiosk Electron shell, black screen with a centered phosphor-green blinking cursor, full keyboard/mouse containment, clean Cmd+Q/Ctrl+Q exit, pointer auto-hide, `npm start`. Plus a dev/prod split (`npm run dev`) so the app can be worked on without fighting the cage.

**Out of scope (must not appear in code):** game logic, audio, typing input handling, world state machine, CRT scanline/flicker effects (those are Phase 3), settings/profiles, packaging/distribution.

**One deliberate addition:** a `--smoke` CLI flag that auto-quits with exit code 0 about 2 seconds after the renderer finishes loading (nonzero on main-process error). It exists solely so headless container/CI verification (`xvfb-run`) can prove the app boots. It is test scaffolding, not a feature.

## File manifest

Every file below must exist at exactly this path with non-trivial content.

```
package.json            name "glyphs", "main": "main.js",
                        scripts: "start" = electron .   |   "dev" = electron . --dev
                        electron is the ONLY dependency (devDependencies, pinned by lockfile)
package-lock.json       generated by npm install
.gitignore              node_modules/, logs, OS junk
main.js                 entry point: parses mode flags (--dev, --smoke), app lifecycle
                        (ready, window-all-closed → quit on all platforms), wires
                        window.js + containment.js, smoke-mode auto-exit
window.js               createWindow(mode) → BrowserWindow.
                        prod: fullscreen + kiosk, resizable/minimizable false,
                        autoHideMenuBar, backgroundColor #000, show on ready-to-show.
                        dev: windowed 1280×800, normal frame.
                        webPreferences (both): contextIsolation true, nodeIntegration false,
                        sandbox true, preload preload.js, devTools only in dev
containment.js          OS-level containment (see layering rubric):
                        Menu.setApplicationMenu(null); before-input-event interception;
                        quit handling; navigation lockdown (will-navigate prevented,
                        setWindowOpenHandler deny) applied via app 'web-contents-created'
preload.js              contextIsolation wiring: contextBridge.exposeInMainWorld with a
                        small frozen, inert host-info object (establishes the bridge
                        pattern for later phases; renderer does not depend on it yet)
renderer/index.html     black screen, centered blinking cursor; loads styles.css and the
                        js files in registration order (namespace → modules → boot);
                        carries a restrictive CSP meta tag (self-only styles/scripts) as
                        standard Electron hygiene
renderer/styles.css     black background; centered phosphor-green (#33ff33) block cursor
                        with steps() blink ≈1.06s cycle and at most a faint glow
                        (no scanlines, no flicker — Phase 3); cursor-hidden state;
                        user-select none
renderer/js/namespace.js  window.Glyphs root: Glyphs.register(name, module) +
                          Glyphs.boot() (calls each module's init() in registration order)
renderer/js/containment.js  DOM-level containment: contextmenu suppressed,
                             dragover/drop suppressed
renderer/js/pointer.js  mouse pointer auto-hide after 2s idle (timer starts at boot),
                        reappears on mousemove
renderer/js/boot.js     calls Glyphs.boot() on DOMContentLoaded
README.md               rewritten: install, start vs dev, macOS containment caveats,
                        namespace how-to
PHASE1-PLAN.md          this file
```

## Containment layering rubric

Each behavior has exactly one owner. No behavior is implemented in both layers.

**Main process owns the OS/chrome surface** (`containment.js`, `window.js`):
- Kiosk + fullscreen + fixed window
- Application menu removed → no menu bar, no default accelerators anywhere
- All keyboard chord interception (`before-input-event`)
- Quit (Cmd+Q / Ctrl+Q → `app.quit()`)
- DevTools policy (`webPreferences.devTools` = dev only)
- Navigation lockdown: no `will-navigate`, no new windows (`setWindowOpenHandler` deny), registered app-wide via `web-contents-created`

**Renderer owns the DOM/input surface** (`renderer/js/`):
- Right-click context menu suppression (DOM `contextmenu`)
- Drag/drop and text-selection suppression
- Mouse pointer auto-hide/show

Renderer registers **no** keyboard handlers in Phase 1; main registers **no** context-menu or pointer logic.

## `before-input-event` decision tree

Evaluated in order for every input event on the window's webContents. `primary` = `input.meta` on macOS, `input.control` elsewhere. Compare `input.key` lowercased (layout-aware: Cmd+Q must follow the layout's Q, so `key`, not `code`).

1. **Quit:** `primary && key === 'q' && !shift && !alt` → `event.preventDefault()`, and on a non-autorepeat `keyDown`, `app.quit()`. We must own quit ourselves: a null application menu removes macOS's built-in Cmd+Q binding.
2. **Dev hatches (dev mode only):** Cmd/Ctrl+R reload; Cmd+Opt+I (mac) / Ctrl+Shift+I / F12 devtools. Handled *programmatically* (`reload()` / `toggleDevTools()` + preventDefault): the null application menu removed every native accelerator, so passing the chord through would do nothing. The macOS devtools chord matches the physical key (`input.code === 'KeyI'`): Option transforms characters on macOS (Option+I is a dead key), so `input.key` never reads 'i' while Alt is held.
3. **Deny-by-default for chords:** anything remaining with `meta` or `control` held → `preventDefault()`. Enumerating bad shortcuts (Cmd+W, Cmd+R, Cmd+Shift+I, Cmd+M, Cmd+H, Cmd+F, Cmd+P, zoom…) is the bug-prone shape; deny-all-chords with one allowlisted exit swallows the ones we didn't think of.
4. **Function keys:** F1–F12 regardless of modifiers → `preventDefault()` (F11 fullscreen toggle, F12 devtools, Alt+F4 lands here as key=F4).
5. **Everything else passes:** plain letters, Shift+letters, arrows, Escape, Enter. Phase 1 ignores them in the renderer, but mash-absorption is a design principle and Phase 3 needs typing.

`globalShortcut` is deliberately **not** used — hijacking system-wide shortcuts is invasive and flaky. On macOS, kiosk mode's presentation options turn out to disable Cmd+Tab, Spotlight, Mission Control, and Force Quit while the app is frontmost (hardware-verified 2026-06). The residual escapes — third-party global hotkeys (e.g. the ChatGPT app's Option+Space launcher), Universal Control pointer drift to nearby devices, and the power button — never reach the app and are documented README limitations with environmental mitigations, not things to code around.

No `close` interception: with Cmd+W swallowed and no visible chrome, close only comes from `app.quit()`, and the quit path must stay clean.

## Renderer namespace pattern

Plain scripts, no bundler, no ES modules (file:// + sandboxed renderer friendly). Every module file follows:

```js
// renderer/js/<name>.js
window.Glyphs.register('<name>', {
  init() { /* wire up listeners */ },
});
```

`index.html` script order: `namespace.js` (defines `window.Glyphs` with `register`/`boot`), then modules (`containment.js`, `pointer.js`), then `boot.js`. The enforced load order **is** the guard — module files call `window.Glyphs.register` directly, with no per-file `window.Glyphs = window.Glyphs || {}` boilerplate (a module without `namespace.js` loaded first is a hard error either way, and the boilerplate would only mask it). Adding a module = new file with `window.Glyphs.register(...)` + one `<script>` tag before `boot.js`.

## Mode matrix

| | `npm start` (prod) | `npm run dev` |
|---|---|---|
| Window | fullscreen + kiosk, fixed | windowed 1280×800, resizable |
| DevTools | disabled at `webPreferences` level AND chords swallowed | enabled; devtools/reload chords handled by the interceptor |
| Shortcut interception | full | full minus dev hatches |
| Renderer containment (right-click, pointer-hide, selection) | on | on |
| Quit | Cmd+Q / Ctrl+Q only | same (plus window close button) |
| `--smoke` | auto-exit 0 after successful load | same |

## Execution order (subagent dispatch)

1. **Chunk 0 (inline):** commit this plan; `.gitignore`.
2. **Chunk A — implementation subagent (Sonnet):** main-process slice: `package.json`, `main.js`, `window.js`, `containment.js`, `preload.js`. Gets the relevant plan sections + instruction to think hard about the decision-tree edge cases. Runs in parallel with B (disjoint files).
3. **Chunk B — implementation subagent (Sonnet):** renderer slice: `index.html`, `styles.css`, `js/namespace.js`, `js/containment.js`, `js/pointer.js`, `js/boot.js`. Gets plan sections + aesthetic notes (warm, slow, phosphor green on black).
4. **Review + commit (inline):** sanity pass over both slices.
5. **Chunk C — verification subagent (Haiku):** `package.json` has `start` + `dev`; `npm install` exits 0; `xvfb-run … npm run dev -- --smoke` boots and exits 0 (with `--no-sandbox` inside the container only). Reports pass/fail with captured exit codes.
6. **Chunk D — audit subagent (Sonnet, fresh context):** reads DESIGN.md, ROADMAP.md, this file; inspects code; checks (a) file manifest, (b) layering rubric with no overlap, (c) dev/prod split, (d) `window.Glyphs.*` consistency, (e) out-of-scope creep. Findings get fixed and the audit re-runs until clean.
7. **README.md (inline):** install, scripts, macOS containment caveats, namespace how-to.
8. **Commit, push** to `claude/cool-bardeen-msipgg`; handoff message with the manual MacBook DoD checklist.
